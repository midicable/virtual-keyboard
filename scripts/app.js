import { renderMainWindow, renderHeader, renderTextArea, renderKeyboard } from './components-render.js';

function main() {
  renderMainWindow();
  renderHeader();
  renderTextArea();
  renderKeyboard();
}

main();